@page
@model Dog_Pet.Pages.Doggy.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Dogs[0].DN)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dogs[0].DB)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dogs[0].PetSID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Dogs) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DN)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DB)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PetSID.PSID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
